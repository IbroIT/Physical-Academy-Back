# IPChain API Documentation

## Описание

API для системы IPChain - blockchain-платформы для защиты интеллектуальной собственности академии.

## Базовый URL

```
http://127.0.0.1:8000/api/ipchain/
```

## Endpoints

### 1. Информация об IPChain

```
GET /api/ipchain/info/?lang=ru
```

**Параметры:**

- `lang` (optional): `ru`, `en`, `kg` - язык ответа

**Ответ:**

```json
{
  "results": [
    {
      "id": 1,
      "title": "IPChain - Защита интеллектуальной собственности",
      "subtitle": "Инновационная blockchain-система для регистрации и защиты патентов",
      "order": 0
    }
  ]
}
```

---

### 2. Статистика IPChain

```
GET /api/ipchain/stats/?lang=ru
```

**Параметры:**

- `lang` (optional): `ru`, `en`, `kg`

**Ответ:**

```json
{
  "results": [
    {
      "id": 1,
      "value": "1000+",
      "label": "Защищено патентов",
      "order": 0
    },
    {
      "id": 2,
      "value": "500+",
      "label": "Активных пользователей",
      "order": 1
    }
  ]
}
```

---

### 3. Патенты

```
GET /api/ipchain/patents/?lang=ru
GET /api/ipchain/patents/{id}/?lang=ru
```

**Параметры:**

- `lang` (optional): `ru`, `en`, `kg`

**Ответ (список):**

```json
{
  "results": [
    {
      "id": 1,
      "number": "PAT2024001",
      "title": "Система мониторинга спортивных показателей",
      "description": "Инновационная технология для отслеживания результатов",
      "full_description": "Разработана комплексная система мониторинга...",
      "status": "Granted",
      "year": "2024",
      "date": "2024-01-15",
      "icon": "📄",
      "technologies": ["IoT", "Machine Learning", "Blockchain"],
      "applications": ["Спортивная медицина", "Тренировочный процесс"],
      "order": 0
    }
  ]
}
```

**Ответ (детально):**

```json
{
  "id": 1,
  "number": "PAT2024001",
  "title": "Система мониторинга спортивных показателей",
  "description": "Инновационная технология для отслеживания результатов",
  "full_description": "Разработана комплексная система мониторинга физических показателей спортсменов...",
  "status": "Granted",
  "year": "2024",
  "date": "2024-01-15",
  "icon": "📄",
  "technologies": ["IoT", "Machine Learning", "Blockchain"],
  "applications": [
    "Спортивная медицина",
    "Тренировочный процесс",
    "Реабилитация"
  ],
  "order": 0
}
```

---

### 4. Функции блокчейна

```
GET /api/ipchain/features/?lang=ru
```

**Параметры:**

- `lang` (optional): `ru`, `en`, `kg`

**Ответ:**

```json
{
  "results": [
    {
      "id": 1,
      "title": "Неизменяемость записей",
      "description": "Невозможность изменения или удаления зарегистрированных данных",
      "icon": "🔒",
      "order": 0
    }
  ]
}
```

---

### 5. Преимущества IPChain

```
GET /api/ipchain/benefits/?lang=ru
```

**Параметры:**

- `lang` (optional): `ru`, `en`, `kg`

**Ответ:**

```json
{
  "results": [
    {
      "id": 1,
      "title": "Быстрая регистрация",
      "description": "Моментальная регистрация патентов и авторских прав в системе",
      "icon": "⚡",
      "order": 0
    }
  ]
}
```

---

## Статусы патентов

- `Granted` - Выдан
- `Pending` - В процессе
- `Active` - Активный

---

## Поддерживаемые языки

- `ru` - Русский (по умолчанию)
- `en` - English
- `kg` - Кыргызча

---

## Примеры использования

### cURL

```bash
# Получить информацию на русском
curl "http://127.0.0.1:8000/api/ipchain/info/?lang=ru"

# Получить патенты на английском
curl "http://127.0.0.1:8000/api/ipchain/patents/?lang=en"

# Получить конкретный патент
curl "http://127.0.0.1:8000/api/ipchain/patents/1/?lang=ru"
```

### JavaScript (Fetch API)

```javascript
// Получить статистику
const response = await fetch(
  "http://127.0.0.1:8000/api/ipchain/stats/?lang=ru"
);
const data = await response.json();
console.log(data.results);

// Получить все патенты
const patents = await fetch(
  "http://127.0.0.1:8000/api/ipchain/patents/?lang=en"
);
const patentsData = await patents.json();
```

### Axios

```javascript
import axios from "axios";

// Получить преимущества
const benefits = await axios.get(
  "http://127.0.0.1:8000/api/ipchain/benefits/",
  {
    params: { lang: "ru" },
  }
);
console.log(benefits.data.results);
```

---

## Управление данными

### Через Django Admin

1. Перейдите на `/admin/`
2. Войдите с учетными данными суперпользователя
3. Найдите раздел "IPChain - Защита интеллектуальной собственности"
4. Добавьте/редактируйте записи с переводами на 3 языка

### Через Management Command

```bash
# Добавить тестовые данные
python manage.py add_ipchain_data
```

---

## Миграции

```bash
# Создать миграции
python manage.py makemigrations ipchain_app

# Применить миграции
python manage.py migrate ipchain_app
```

---

## Модели

### IPChainInfo

Основная информация о системе IPChain

### IPChainStatistic

Статистические показатели

### Patent

Патенты с детальной информацией

### BlockchainFeature

Функциональные возможности блокчейна

### IPChainBenefit

Преимущества использования системы

Все модели имеют поддержку переводов на 3 языка (ru, en, kg).
